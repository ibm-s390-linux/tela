# Check if mix of wildcard -> non-wildcard requirement works.
#
# test:   [ "$TELA_SYSTEM_DUMMY_a_SIZE" == 1 ]
# test:   [ "$TELA_SYSTEM_DUMMY_0_SIZE" == 2 ]
# test:   [ "$TELA_SYSTEM_DUMMY_1_SIZE" == 3 ]
# result: ^ok[^#]*$
# rc: triple.rc

dummy *:
dummy a:
